version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next  # ← ここが重要です
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
